{
    "schema": 1,
    "type": "completion",
    "description": "create Azure Resource Graph query based on user input.",
    "execution_settings": {
        "default": {
            "max_tokens": 800,
            "temperature": 0
        }
    },
    "input_variables": [
        {
            "name": "input",
            "description": "The user's request",
            "required": true
        }
    ]
}